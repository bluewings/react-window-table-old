---
name: WithColumns
---

import { PropsTable, Playground } from 'docz';
import WindowTable from '../components/WindowTable';
import { columns, rows } from '../examples/data/fake-users';
import withColumns from './with-columns.enhancer';

# WithColumns (HOC)

<Playground>
  <WindowTable
    width={760}
    height={400}
    columns={columns}
    rows={rows}
    enhancer={withColumns}
              
    rowHeight={50}
    columnWidth={120}
    fixedTopCount={1}
    fixedLeftCount={3}
  >
  </WindowTable>
</Playground>