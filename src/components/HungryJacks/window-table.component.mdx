---
name: WindowTable
---


import { PropsTable, Playground } from 'docz';
import WindowTable from './window-table.component.jsx';
import { columns, rows } from '../../examples/data/pokedex';

# WindowTable

React components for efficiently rendering large lists and tabular data React components for efficiently rendering large lists and tabular data React components for efficiently rendering large lists and tabular data

React components for efficiently rendering large lists and tabular data


## Getting Started


## Properties

<PropsTable of={WindowTable} />

console.log(WindowTable)

## Playground

<Playground>
    <WindowTable
          width={600}
          height={400}
          columnCount={50}
          rowCount={100}
          rowHeight={50}
          columnWidth={100}
          fixedLeftCount={1}
          fixedRightCount={1}
          fixedTopCount={1}
          fixedBottomCount={1} >
          {({ columnIndex, rowIndex, style }) => {
           return (
       <div className="cell" style={style}>
                      {rowIndex} , {columnIndex}
                    </div>
           )  
          }}
          </WindowTable>
        
</Playground>
